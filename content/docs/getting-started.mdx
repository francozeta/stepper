---
title: "Getting Started"
description: "Install Stepper through the shadcn registry and copy the component source into your app."
eyebrow: "basics"
navTitle: "Install"
navIcon: "book-open"
copyImport: "import { Stepper } from \"@/components/ui/stepper\";"
copyLabel: "Copy import"
copyCopiedLabel: "Import copied"
---

import { CodeBlock, FileTree, Section } from "@/components/docs-content";
import {
  controlledSnippet,
  gettingStartedSnippet,
  registryInstallSnippet,
  usageSnippet,
} from "@/lib/docs";

<Section
  title="Install"
  description="Stepper is installed through the shadcn registry. The CLI copies the component source into your project, so you can customize it directly."
>
  <CodeBlock
    code={registryInstallSnippet}
    filename="terminal"
    lang="bash"
    showLineNumbers={false}
    compact
  />
</Section>

<Section
  title="What gets added"
  description="Stepper does not install as a runtime package. After installation, the source belongs to your project."
>
  <FileTree
    items={[
      {
        name: "components",
        children: [
          {
            name: "ui",
            children: [{ name: "stepper.tsx" }],
          },
        ],
      },
    ]}
  />
  <CodeBlock
    code={usageSnippet}
    filename="app/page.tsx"
    lang="tsx"
  />
</Section>

<Section
  title="Use it uncontrolled"
  description="Use defaultValue when the Stepper can own its active step."
>
  <CodeBlock code={gettingStartedSnippet} filename="checkout-stepper.tsx" />
</Section>

<Section
  title="Use it controlled"
  description="Pass value and onValueChange when app state, validation, or routing decides the current step."
>
  <CodeBlock code={controlledSnippet} filename="controlled-usage.tsx" />
</Section>
