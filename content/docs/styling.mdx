---
title: "Styling"
description: "Stepper uses shadcn/ui theme tokens, data attributes, and local source ownership for styling."
eyebrow: "guides"
---

import { CodeBlock, Section } from "@/components/docs-content";
import { Badge } from "@/components/ui/badge";
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import {
  indicatorCode,
  stateSelectorsCode,
  themeTokensSnippet,
} from "@/lib/docs";

export const tokens = [
  "background",
  "foreground",
  "border",
  "muted",
  "muted-foreground",
  "primary",
  "primary-foreground",
  "destructive",
  "destructive-foreground",
  "ring",
];

<Section
  title="Theme tokens"
  description="The copied component avoids one-off color values and leans on semantic Tailwind tokens."
>
  <div className="flex flex-wrap gap-2">
    {tokens.map((token) => (
      <Badge key={token} variant="outline" className="font-mono">
        {token}
      </Badge>
    ))}
  </div>
</Section>

<Section
  title="Theme token overrides"
  description="Stepper reads the same semantic tokens as the rest of your shadcn/ui project."
>
  <CodeBlock code={themeTokensSnippet} filename="globals.css" lang="css" />
</Section>

<Section title="State selectors">
  <CodeBlock
    code={stateSelectorsCode}
    filename="state-selectors.css"
    lang="css"
  />
</Section>

<Section
  title="Customize by composition"
  description="Because Stepper is copied into your app, you can pass className, target data attributes, or edit components/ui/stepper.tsx directly."
>
  <Card>
    <CardHeader>
      <CardTitle>Indicator slot</CardTitle>
      <CardDescription>
        Pass custom children to StepperIndicator for lucide icons, numbers, or status marks.
      </CardDescription>
    </CardHeader>
    <CardContent>
      <CodeBlock
        code={indicatorCode}
        filename="custom-indicator.tsx"
        className="border-0 shadow-none"
      />
    </CardContent>
  </Card>
</Section>
