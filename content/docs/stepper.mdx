---
title: "Stepper"
description: "A composable primitive for building accessible multi-step flow architecture with local source ownership."
eyebrow: "component"
badge: "beta"
navTitle: "Stepper"
navIcon: "boxes"
copyImport: "import { Stepper } from \"@/components/ui/stepper\";"
copyLabel: "Copy import"
copyCopiedLabel: "Import copied"
---

import { CodeBlockCommand } from "@/components/code-block-command";
import { DocsExample } from "@/components/docs-example";
import { CodeBlock, Section } from "@/components/docs-content";
import { File, Files, Folder } from "@/components/files";
import { PropsTable } from "@/components/props-table";
import { StepperCheckoutExample } from "@/components/stepper-examples";
import {
  checkoutExampleCode,
  contentProps,
  itemProps,
  listProps,
  navigationProps,
  registryInstallCommands,
  rootProps,
  stepperCompositionTree,
  stepperHeadlessCompositionTree,
  stepperUsageSnippet,
  triggerProps,
} from "@/lib/docs";

<Section
  title="Component preview"
  description="A checkout flow rendered with the core primitives only: ordered steps, state-aware indicators, and navigation controls."
>
  <DocsExample
    title="Checkout flow"
    description="A compact preview that starts at the cart and advances one step at a time."
    badge="Primitive"
    code={checkoutExampleCode}
    filename="checkout-flow.tsx"
    preview={<StepperCheckoutExample compact />}
    previewClassName="bg-[#050505]"
  />
</Section>

<Section
  title="Installation"
  description="Add the primitive through the shadcn registry. The command copies the source into your app."
>
  <CodeBlockCommand {...registryInstallCommands} />
</Section>

<Section
  title="Usage"
  description="Import the primitive parts you need, then compose the list, items, and controls directly in JSX."
>
  <div className="grid gap-4">
    <CodeBlock code={stepperUsageSnippet} filename="imports.tsx" />
    <CodeBlock code={checkoutExampleCode} filename="checkout-flow.tsx" />
  </div>
</Section>

<Section
  title="Files"
  description="The registry installs a single editable primitive into your local component folder."
>
  <Files>
    <Folder name="components" defaultOpen>
      <Folder name="ui" defaultOpen>
        <File name="stepper.tsx" />
      </Folder>
    </Folder>
  </Files>
</Section>

<Section
  title="Composition"
  description="Use the following composition to build a Stepper."
>
  <CodeBlock
    code={stepperCompositionTree}
    filename="stepper-composition.txt"
    lang="text"
    showLineNumbers={false}
  />
</Section>

<Section
  title="Headless composition"
  description="Hide the visual list and drive custom controls with useStepper when the product surface needs a different layout."
>
  <CodeBlock
    code={stepperHeadlessCompositionTree}
    filename="headless-composition.txt"
    lang="text"
    showLineNumbers={false}
  />
</Section>

<Section
  title="API Reference"
  description="The public surface stays small: root state, ordered list, item state, trigger composition, content panels, and navigation helpers."
>
  <div className="grid gap-6">
    <PropsTable rows={rootProps} />
    <PropsTable rows={listProps} />
    <PropsTable rows={[...itemProps, ...triggerProps]} />
    <PropsTable rows={contentProps} />
    <PropsTable rows={navigationProps} />
  </div>
</Section>
