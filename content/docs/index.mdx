---
title: "Introduction"
description: "Architecture notes for composing Stepper into real product flows without coupling the primitive to forms, routes, or validation."
eyebrow: "docs"
badge: "beta"
navTitle: "Introduction"
navIcon: "book-open"
---

import Link from "next/link";
import { ArrowRight } from "lucide-react";

import { CodeBlock, InfoGrid, Section } from "@/components/docs-content";
import { Button } from "@/components/ui/button";
import {
  flowArchitecture,
  stepperCompositionTree,
  worksWith,
} from "@/lib/docs";

<Section
  title="Flow architecture"
  description="Stepper is the progress layer. Your app keeps the business rules, validation, routing, persistence, and async work."
>
  <InfoGrid items={flowArchitecture} />
</Section>

<Section
  title="Composition patterns"
  description="Start with the primitive structure, then replace the visual parts that need product-specific behavior."
>
  <CodeBlock
    code={stepperCompositionTree}
    filename="stepper-composition.txt"
    lang="text"
    showLineNumbers={false}
  />
</Section>

<Section
  title="Integration boundary"
  description="Adapters sit around the primitive. They feed state into Stepper instead of making Stepper own the entire workflow."
>
  <InfoGrid items={worksWith} />
</Section>

<Section
  title="Next"
  description="Install the source, then open the component reference when you need examples, composition, and API details."
>
  <div className="grid gap-3 sm:grid-cols-2">
    <Button asChild variant="outline" size="sm" className="h-8 w-full justify-between rounded-none border-white/10 bg-[#050505] px-3 text-zinc-500 hover:bg-white/[0.045] hover:text-zinc-100">
      <Link href="/docs/installation">
        Installation
        <ArrowRight data-icon="inline-end" />
      </Link>
    </Button>
    <Button asChild variant="outline" size="sm" className="h-8 w-full justify-between rounded-none border-white/10 bg-[#050505] px-3 text-zinc-500 hover:bg-white/[0.045] hover:text-zinc-100">
      <Link href="/docs/stepper">
        Stepper
        <ArrowRight data-icon="inline-end" />
      </Link>
    </Button>
  </div>
</Section>
