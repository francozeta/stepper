---
title: "Stepper"
description: "A shadcn registry component for guided multi-step flows in React, Next.js, and Tailwind CSS projects."
eyebrow: "components/ui"
badge: "shadcn registry"
---

import Link from "next/link";
import { ArrowRight } from "lucide-react";

import { DocsExample } from "@/components/docs-example";
import { CodeBlock, FileTree, InfoGrid, Section } from "@/components/docs-content";
import { StepperExample } from "@/components/stepper-examples";
import { Button } from "@/components/ui/button";
import {
  registryInstallSnippet,
  workspaceExampleCode,
  whyStepper,
} from "@/lib/docs";

<Section
  title="Install"
  description="Stepper is distributed through the shadcn registry. The CLI copies the component source into your app, so you own the code and can customize it directly."
>
  <div className="grid gap-4">
    <CodeBlock
      code={registryInstallSnippet}
      filename="registry"
      lang="bash"
      showLineNumbers={false}
      compact
    />
    <FileTree
      items={[
        {
          name: "components",
          children: [
            {
              name: "ui",
              children: [{ name: "stepper.tsx" }],
            },
          ],
        },
      ]}
    />
  </div>
</Section>

<Section
  title="Preview"
  description="A real workspace setup flow where the Stepper blocks progress, marks errors, and unlocks later steps as requirements are completed."
>
  <DocsExample
    title="Create workspace"
    description="The active step has a missing slug, preferences are locked, and invites only unlock after defaults are saved."
    badge="Product flow"
    code={workspaceExampleCode}
    filename="workspace-setup.tsx"
    preview={<StepperExample />}
  />
</Section>

<Section
  title="Why another Stepper?"
  description="Most steppers try to own the workflow. This one gives you composable UI primitives, while your app owns routing, validation, form state, persistence, and server actions."
>
  <InfoGrid items={whyStepper} />
</Section>

<Button asChild size="lg" className="w-fit">
  <Link href="/patterns">
    View recipes
    <ArrowRight data-icon="inline-end" />
  </Link>
</Button>
