---
title: "Examples"
description: "Focused Stepper demos for intent onboarding, product setup, checkout, vertical onboarding, and controlled React state."
eyebrow: "api"
---

import { DocsExample } from "@/components/docs-example";
import {
  StepperCheckoutExample,
  StepperControlledExample,
  StepperExample,
  StepperVerticalExample,
} from "@/components/stepper-examples";
import { StepperIntentOnboardingExample } from "@/components/stepper-intent-onboarding";
import {
  checkoutExampleCode,
  controlledExampleCode,
  verticalExampleCode,
  workspaceExampleCode,
} from "@/lib/docs";

<div className="flex flex-col gap-6">
  <DocsExample
    title="Intent onboarding flow"
    description="Model intent-based onboarding with hidden steps, profile validation, branching, interests, async setup, and a signed-in completion state."
    badge="Block"
    code={"pnpm dlx shadcn@latest add @stepper/stepper-intent-onboarding"}
    filename="registry"
    lang="bash"
    preview={<StepperIntentOnboardingExample />}
    previewClassName="bg-background p-0 sm:p-0"
  />

  <DocsExample
    title="Create workspace"
    description="Show a product setup flow with blockers, completion, and disabled future steps."
    badge="Product flow"
    code={workspaceExampleCode}
    filename="workspace-setup.tsx"
    preview={<StepperExample />}
  />

  <DocsExample
    title="Checkout flow"
    description="Guide customers through cart review, shipping details, and payment."
    badge="Horizontal"
    code={checkoutExampleCode}
    filename="checkout-flow.tsx"
    preview={<StepperCheckoutExample />}
  />

  <DocsExample
    title="Workspace onboarding"
    description="Use a vertical layout when each step needs more context and scanning."
    badge="Vertical"
    code={verticalExampleCode}
    filename="workspace-onboarding.tsx"
    preview={<StepperVerticalExample />}
  />

  <DocsExample
    title="Controlled review"
    description="Drive the active step from React state when the flow depends on app logic."
    badge="Controlled"
    code={controlledExampleCode}
    filename="controlled-stepper.tsx"
    preview={<StepperControlledExample />}
  />
</div>
